#include <iostream>
#include <cstring>
using namespace std;

bool isAnagram(string s1, string s2){
    if(s1.length() != s2.length()){
        return false;
    }
    int n=s.length();
    char ch1[n+1];
    char ch2[n+1];
    strcpy(ch1, s1.c_str());
    strcpy(ch2, s2.c_str());

}

int main(){

    return 0;
}